let Koa,Router,bodyParser,mongoose;_8db‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_8db‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_8db‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_8db‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);require('dotenv').config();





const api = require('./api');

// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const {PORT, MONGO_URI} = process.env;

mongoose
.connect(MONGO_URI)
    .then(() => {
        console.log('Connected to MongoDB');
    })
    .catch(e => {
        _8db‍.g.console.error(e);
    });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());   // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
    _8db‍.g.console.log('Listening to part %d', port);
});